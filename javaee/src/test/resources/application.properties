# 从 setting.xml 引入。
repo=${settings.localRepository}
# 从 pom.xml 引入。
name=${project.name}
# 从 pom.xml 中 properties 引入。
message=${my.filter.value}
# 从外部文件引入。
filters=${message}
# 从命令中引入。mvn process-resources "-Dname=hello filters"
cmd=${name}