# 从 setting.xml 引入。
repoDir=${settings.localRepository}
# 从 pom.xml 引入。
projectName=${project.name}
# 从 pom.xml 中 properties 引入。
pomProperty=${my.pom.filter.value}
# 从外部文件引入。
fileProperty=${my.file.filter.value}
# 从命令中引入。mvn process-resources "-Dmy.command.filter.value=hello filters"
cmdProperty=${my.command.filter.value}
# 项目文件路径。
root=${basedir}
